<%= link_to 'Back', project_path(@feature.project, @feature), class:"btn btn-secondary mb-3" %>

<div class="card">
  <div class="card-body">
    <h2 class="card-title mb-4">
      <span>New Feture </span>
    </h2>
    <%= render 'form', project: @project %>
  </div>
</div>
